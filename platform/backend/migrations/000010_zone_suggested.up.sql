-- Ф8-R3: zero-shot предсказание зоны по фото (Marqo). Храним ОТДЕЛЬНО от
-- garment_zone — принцип «флаг на ревью, не слепая перезапись»: где партнёр
-- дал категорию, сравниваем; где не дал (NULL) — заполняем предсказанием.
ALTER TABLE products
    ADD COLUMN zone_suggested TEXT;   -- предсказанная зона по фото (аудит расхождений)
